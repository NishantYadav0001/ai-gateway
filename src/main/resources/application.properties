# 1. GROQ CONFIGURATION
# Keep this URL fix (No /v1 at the end)
spring.ai.openai.base-url=https://api.groq.com/openai
# REMOVE your real key if it is here, use the placeholder:
spring.ai.openai.api-key=${GROQ_API_KEY}
spring.ai.openai.chat.options.model=llama-3.1-8b-instant
spring.ai.openai.embedding.enabled=false

# 2. PINECONE CONFIGURATION
# REMOVE your real key, use the placeholder:
spring.ai.vectorstore.pinecone.api-key=${PINECONE_API_KEY}
# KEEP this environment fix! This is crucial for HF.
spring.ai.vectorstore.pinecone.environment=aped-4627-b74a
spring.ai.vectorstore.pinecone.index-name=gateway-cache
# KEEP your project ID fix!
spring.ai.vectorstore.pinecone.project-id=3vqcatd
spring.ai.vectorstore.pinecone.initialize-schema=false

# 3. DATABASE CONFIGURATION
# KEEP this URL exactly as is
spring.datasource.url=jdbc:postgresql://ep-bitter-pine-ap3c68qc.c-7.us-east-1.aws.neon.tech/neondb?sslmode=require
# REMOVE neondb_owner and your real password, use the placeholders:
spring.datasource.username=${DB_USER}
spring.datasource.password=${DB_PASS}
spring.datasource.driverClassName=org.postgresql.Driver

# OTHER SETTINGS
spring.sql.init.mode=always
spring.main.lazy-initialization=true
management.endpoints.web.exposure.include=health